const newButton = document.createElement("BUTTON");
newButton.innerHTML = "Alternative treatment";
    
const insertionPoint = document.querySelector(".caption-control button");
insertionPoint.parentNode.insertBefore(newButton, insertionPoint);

// Do not change the code above, imagine it's running from a previous script on the page.
// Using only standard JavaScript, no libraries complete the following request

